name: number_to_thai_words
description: |
  A Dart package for converting numbers into Thai words.

version: 1.0.3
homepage: https://github.com/thisadee2897/number_to_thai_words
repository: https://github.com/thisadee2897/number_to_thai_words

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

platforms:
  android:
  ios:
  web:
  linux:
  macos:
  windows: